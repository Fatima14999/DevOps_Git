name: Openshift/Openshift
on:
  workflow_dispatch:
jobs:
  build:
    runs-on:
      - self-hosted
      - ubaidagent
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
#     # There was an internal error transforming this item
#     # Message: There was an error transforming TerraformInstaller@1 with a custom transformer
#     - TerraformInstaller@1:
#         terraformVersion: 1.5.0
